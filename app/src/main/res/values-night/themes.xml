<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JobMatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <!-- Cores primárias tema escuro -->
        <item name="colorPrimary">@color/primaria</item>
        <item name="colorPrimaryVariant">@color/preto</item>
        <item name="colorOnPrimary">@color/branco</item>

        <!-- Cores secundárias tema escuro -->
        <item name="colorSecondary">@color/secundaria</item>
        <item name="colorSecondaryVariant">@color/azul_escuro_oceano</item>
        <item name="colorOnSecondary">@color/branco</item>

        <!-- Cores de fundo -->
        <item name="android:colorBackground">@color/cinza_escuro</item>
        <item name="colorSurface">@color/preto_cinza</item>

        <!-- Cor da barra de status -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Cores de textos -->
        <item name="colorOnBackground">@color/branco</item>
        <item name="colorOnSurface">@color/branco</item>
        <item name="colorOnError">@color/branco</item>
        <item name="android:textColorPrimary">@color/branco</item>
        <item name="android:textColorSecondary">@color/cinza_claro</item>
        <item name="android:textColorHint">@color/cinza_claro</item>

        <!-- Cor de erro -->
        <item name="colorError">@color/erro</item>
    </style>
</resources>